<%= link_to("back to", sections_path, :class=>"back-link")%>
<div class="subjects new">
	<h2>create section</h2>
	<%= form_for(@section) do |f| %>

	<%= render(:partial=>"form", :locals=>{:f=>f}) %>
	<div class="submit">
		<%= f.submit("Create Section") %>
	</div>
	<% end %>
	</div>
	