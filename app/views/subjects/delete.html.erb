<%= link_to("back_to",subjects_path)%>
<h1>Delete Subject</h1>
<%= form_for(@subject,:method=>"delete") do |f|%>
<h1>Are you sure you want to delete?</h1>
<p><%= f.object.name %></p>
<div class="submit">
	<%= f.submit("Delete Subject")%>
	</div>
	<% end %>

